# Learnings Index

Per-repo learnings captured by the `/learn` skill. Each entry points to a
file in this directory. The active ruleset is synced into the repo's
`CLAUDE.md` under `## Learnings`.

- [prd-refine missing migration sub-tasks](skill-prd-refine-missing-migrations.md) — prd-refine omits migration sub-tasks when the plan touches a model file.
